<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:windows="clr-namespace:Xamarin.Forms.PlatformConfiguration.WindowsSpecific;assembly=Xamarin.Forms.Core"
             xmlns:ia="clr-namespace:IntelliAbb.Xamarin.Controls;assembly=IntelliAbb.Xamarin.Controls"
             Title="{Binding Title}" 
             x:Class="TicketsDemo.Views.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ADD" Command="{Binding AddCommand}"/>
    </ContentPage.ToolbarItems>
        <ListView ItemsSource="{Binding Tickets}" HasUnevenRows="true" ItemSelected="Handle_ItemSelected" IsPullToRefreshEnabled="True" RefreshCommand="{Binding RefreshCommand}" IsRefreshing="{Binding IsBusy}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="*"/>
                              <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            
                            <StackLayout Spacing="0">
                                <Label Text="{Binding Title}" FontSize="Large"/>
                                <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Priority, StringFormat='Priority: {0}'}" FontSize="Micro" TextColor="Gray" HorizontalOptions="FillAndExpand"/>
                                <Label Text="{Binding AssignedTo, StringFormat='Assigned To: {0}'}" FontSize="Micro" TextColor="Gray" HorizontalOptions="FillAndExpand"/>
                                </StackLayout>
                            </StackLayout>
                            <ia:Checkbox Grid.Column="1" IsChecked="{Binding IsCompleted, Mode=TwoWay}" FillColor="{StaticResource Accent}" OutlineColor="{StaticResource Accent}"/>

                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="Handle_Tapped" Command="{Binding TicketTappedCommand}" CommandParameter="{Binding .}"/>
                        </Grid.GestureRecognizers>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
</ContentPage>